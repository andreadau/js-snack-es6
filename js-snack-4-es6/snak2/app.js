//1

 palla = {
    nome : palla,
    peso : Number(prompt("metti il peso della palla"))
}
console.log(palla);

//2
let massa = Number(prompt("Metti il peso"));
palla = {
    nome : "palla",
    peso : massa
}
console.log(palla);